/** @type {import('tailwindcss').Config} */
export default {
    content: [
        './index.html', // If you're using Vite, this includes the HTML files
        './src/**/*.{vue,js,ts,jsx,tsx}', // Include Vue, JS, TS, JSX, TSX files in your src folder
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
